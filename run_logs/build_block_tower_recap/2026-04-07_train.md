# block tower recap — 4-way advantage conditioning experiment

## Mode
- run_type: experiment
- objective: train block tower recap models across 4 advantage conditioning variants (positive-only, mixed) × (flat prompt, hierarchical subtask prompt) to compare recap effectiveness

## Config
- script: `slurm/train_build_block_tower_recap_slurm.sh <mode>`
- dataset: 6-mix block tower (`villekuosmanen/build_block_tower` + 5 dAgger rounds) on HuggingFace
- assets bootstrapped from: `pravsels/pi05-build-block-tower-6mix` checkpoints/49999/assets (norm stats + valid indices)
- key settings: 17D action space, delta actions, per-timestep action normalization, advantage dropout `0.3`, `num_train_steps=50_000`, `batch_size=36`, 4 GPUs per job

### Variants

| mode | config | model |
|------|--------|-------|
| `positive_only` | `pi05_build_block_tower_recap_positive_only` | Pi0Config(pi05=True) |
| `mixed` | `pi05_build_block_tower_recap_mixed` | Pi0Config(pi05=True) |
| `subtask_positive_only` | `pi05_build_block_tower_subtask_recap_positive_only` | Pi05Config (subtask_loss_weight=1.0) |
| `subtask_mixed` | `pi05_build_block_tower_subtask_recap_mixed` | Pi05Config (subtask_loss_weight=1.0) |

## Job — positive_only
- job_id: 3664209
- submitted: `2026-04-07T20:05:00Z`
- start_human: Tuesday, Apr 7th, 2026

## Job — mixed
- job_id: 3664210
- submitted: `2026-04-07T20:05:00Z`
- start_human: Tuesday, Apr 7th, 2026

## Job — subtask_positive_only
- job_id: 3664211
- submitted: `2026-04-07T20:05:00Z`
- start_human: Tuesday, Apr 7th, 2026

## Job — subtask_mixed
- job_id: 3664212
- submitted: `2026-04-07T20:05:00Z`
- start_human: Tuesday, Apr 7th, 2026

## Status
- 2026-04-07 19:49 UTC — first submission (jobs 3660655–3660658) failed; all 4 crashed within ~2 min due to string metadata fields (`control_mode`, `dataset_repo_id`) surviving to collation/JAX
- 2026-04-07 20:05 UTC — fix committed (`c83d4fa`): strip `control_mode` from `SetAdvantageLabelFromControlMode` output, remove `dataset_repo_id` from `_copy_passthrough_metadata`; resubmitted as jobs 3664209–3664212; all pending (resources)
- 2026-04-08 UTC — repeated subtask OOMs persisted after batch-size reductions (`28`, `24`, `16`), with failures around ~74 GB then extra allocation attempts (JAX allocator behavior).
- 2026-04-08 UTC — mitigation committed (`c4211a5`): set `XLA_PYTHON_CLIENT_PREALLOCATE=false` and `XLA_PYTHON_CLIENT_ALLOCATOR=platform` in slurm env; add `--fsdp-devices=4` for subtask modes.
- 2026-04-08 UTC — cancelled prior subtask jobs (3700309, 3700310), resubmitted as 3700315 (`subtask_positive_only`) and 3700316 (`subtask_mixed`); both started running.
- 2026-04-08 UTC — subtask batch-size ramp started after stability at step 100: committed `50c66f7` (set `subtask_*` to `--batch-size=32 --fsdp-devices=4`), submitted jobs 3700331 and 3700332.
- 2026-04-08 UTC — increased subtask batch to 64 (`670a2cd`), replaced with jobs 3700347 and 3700348.
- 2026-04-08 UTC — increased subtask batch to 128 (`86f8bdf`), replaced with jobs 3700374 (`subtask_positive_only`) and 3700375 (`subtask_mixed`), currently queued/ramping.

## Results

## W&B

## Next
- monitor 3700374 and 3700375 for first 200 steps and memory behavior at `batch-size=128`
- if stable at `batch-size=128`, keep as default for subtask variants in this experiment
- if healthy, let run to 50k steps and compare loss/eval across the 4 variants
